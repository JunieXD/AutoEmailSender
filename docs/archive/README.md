# Documentation Archive

This directory preserves historical specifications and implementation plans. They are not current architecture, product, or operations guidance.

Paths, commands, and module names inside archived documents describe the repository topology that existed when each document was written. They are intentionally not rewritten during later reorganizations. Current work must follow `docs/README.md`, `docs/architecture/`, and the active owner directories.
