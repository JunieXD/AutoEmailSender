interface:
  display_name: "AutoEmailSender Release"
  short_description: "Certify stable and prerelease builds without rebuilding."
  default_prompt: "Use $auto-email-sender-release to prepare, certify, publish, and verify an exact AutoEmailSender stable or prerelease candidate."
