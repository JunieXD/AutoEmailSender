{
  "name": "auto-email-sender-desktop",
  "version": "2.3.9",
  "private": true,
  "type": "module",
  "main": "dist/src/main.js",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.preload.json --noEmit",
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.preload.json",
    "test": "vitest run",
    "dev": "npm run build && electron . --dev",
    "setup:sparkle": "../scripts/setup-sparkle.sh",
    "build:sparkle": "../scripts/build-sparkle-bridge.sh",
    "prepare:sparkle": "npm run setup:sparkle && npm run build:sparkle",
    "pack": "npm run build && electron-builder --config electron-builder.yml --win --dir",
    "pack:mac": "npm run prepare:sparkle && npm run build && electron-builder --config electron-builder.yml --mac --dir --publish never",
    "dist": "npm run build && electron-builder --config electron-builder.yml --win nsis --publish never",
    "dist:mac": "npm run prepare:sparkle && npm run build && electron-builder --config electron-builder.yml --mac dmg --publish never",
    "publish": "npm run build && electron-builder --config electron-builder.yml --win nsis --publish always"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "electron": "^39.2.7",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
